# Independent publication scope review

Final status: PASS, with no remaining publication-scope blocker.
Scientific source checkpoint: `d92d08d66e3282db9b525aa46b6f50ab09c73569`.
The source repository is clean. The review began at `78f6ac6` plus the 19
then-untracked projection files and was completed against the committed
source and the updated public overview, summary, status and wrapper.
This is a bounded independent internal review, read-only with respect to both
repositories. It is not external human peer review. No solver or archived
arithmetic replay was run by this reviewing agent.

## Source findings

The current CLAIM-0011 has the correct original physical contact/vertex
correspondence, strict relative-interior facet-contact hypothesis, actual
Y[0,3] extrema, and both canonical height orders. It proves the common strict
bound `w<(5/4)(1+2/sqrt(3))` for positive contact determinant. At width at
least that threshold, the strict negative determinant and the non-strict
physical observer clause `F03>=3F01+2F02` are necessary. The clause means
that the original observer (3,1,1) is on or beyond the facet-0 supporting
plane, not necessarily in the facet. The earlier triangle proof remains a
valid weaker result; the rectangle supplement supports the improved bound.
Neither proof excludes the remaining exceptional negative branch, other
Y-extrema charts, or the entire determinant-five contact class.

The compact interval compiler's checked interior six-shape box is a real
continuous-box exclusion with a reference-bound CPC/Ethos receipt. Its
independent review correctly identifies that one horizontal gauge envelope
already refutes this box. It must not be presented as a solution of the full
fiber interaction. The two whole-cube SAT outer models are not actual bodies.
The two later forced-observer SAT matrices have actual extrema outside the
requested Y[0,3] chart; they are not counterexamples to the sign theorem.

The new horizontal projection is exact for its stated necessary relaxation,
with fixed four-base coordinates and twelve strict piece-selection branches
per order. The strict Farkas criterion, small-support enumeration, and
closed-polygon barycenter method preserve strict-boundary semantics. The
a=1 strictification is explicitly scoped to existence when the other
retained inequalities are strict; it is not a license to erase arbitrary
endpoint equalities in the full geometric model.
The independent archive audit records 864 coefficient identities, 120
initial branches and 264 additional probe branches, and 160 generic rational
kernel controls. The additional four point obstructions are not a cover of
base space, and feasible subsystem witnesses are not hollow-body witnesses.

The forward reduction from twelve to four selections is an equivalence only
under the declared normal-form domain and Q>0. The pure-base inequalities
are necessary cuts, not equivalent replacements for all gauge requirements.
The exceptional a=1,y=1 strict-contact issue and the q=0 polynomial endpoint
are handled explicitly. The exact H2 subsystem projection concerns only its
listed inequalities; it does not reconstruct all geometry or solve the full
four-gauge problem.

The new closed four-base box has a universal monotonic endpoint proof:
`f<=3597/2048<183/100` and `B<=2277/4096<183/200` eliminate the surviving
forward first-gauge alternatives. All admissible remaining parameters are
excluded within this box. Its compact-coordinate volume `1/1048576` is not
body volume, residual-body measure, or removed Y area. Its height enclosure
is not itself an excluded Y rectangle: the C,E restrictions are essential.
The independent review verifies disjointness from all nine previous closed
Y rectangles and satisfaction of the listed older necessary base cuts.
This is a partial continuous domain exclusion, not a whole-class result.

The asymmetric forward matrix is correctly limited: all four chosen gauges
and the retained observer conditions pass, but actual Y width is `25/14`.
Its full width is therefore below the global target regardless of whether
it is hollow. No larger scope is claimed for that fixture.

## Publication wording to preserve

1. Keep the original physical Y[0,3] chart and strict facet-contact hypotheses
   adjacent to the new sign theorem; the determinant sign is not universal
   over arbitrary orderings or other charts.
2. Distinguish the six-shape exact full-target fibers from the four-base
   horizontal necessary relaxation. The latter is not a complete geometric
   reduction to a solved four-variable problem.
3. Describe both new continuous boxes by their parameter restrictions and
   preserve the measure caveats. A new height-only rectangle must not be
   inferred from the four-base box's height enclosure.
4. Keep 58 necessary contact types and the global flatness bounds unchanged;
   internal AI review and external proof-checking software are not external
   human peer review. Priority is unconfirmed.
5. Default archive/hash replay is not fresh Ethos proof-kernel checking.
   Preserve that distinction in the public validation instructions.

## Resolved presentation points

The latest French summary now states the prescribed relative-interior
opposite-facet contacts next to CLAIM-0011 and explicitly uses global width
>17/5 for the new box. The public NEXT and research-status documents have
an opening public-snapshot notice identifying source d92d08d and explaining
that local-only statements below belong to historical research checkpoints.
The new box proof and finite-probe references have been copied. A read-only
local-link scan of README, FINAL_SUMMARY, NEXT, research status and the box
proof found no missing local targets.

## Updated public presentation and wrapper review

The newly updated public README preserves the correct relative-interior
opposite-facet contact hypothesis adjacent to CLAIM-0011, its Y[0,3] scope,
the common strict determinant-positive width bound, and the necessary
negative orientation. It correctly distinguishes the four-base horizontal
necessary relaxation from full-body feasibility and states that the reverse
order and remaining continuous domain stay open. The new parameter box and
its measure/height-enclosure caveats are correctly stated. Its approximate
constant 2.693375673 is consistent with the exact value.

The default projection wrapper runs nine exact/symbolic stages, adds two
encoding audits only with --encodings, and adds one fresh external proof
check only with all three pinned-checker paths. It rejects optimized Python
and restores original scientific JSON/SMT/proof bytes after each subprocess.
Its own fresh report retains subprocess stdout, return codes, regenerated
receipt hashes, and the requested/actual kernel-check distinction.
No hidden SMT search was identified in the new default projection generator
or the independently audited kernel.

The public-only --skip-primary-text adaptation is explicit, skips only the
unredistributed ACMS text read/binding, and reports
`primary_text_check=SKIPPED_UNREDISTRIBUTED`. Arithmetic and available local
source bindings still run. Restoring the original source-review receipt is
acceptable because the fresh wrapper log exposes the skipped step rather
than claiming a fresh primary-source inspection. This explicitly skipped step is distinct from the earlier public
CLAIM-0010 adaptation. The original identified primary-source review remains
archived; default public reproduction does not newly inspect the omitted
article.

## Fresh public validation evidence inspected

The saved `results/public_det5_projection_validation.json` reports PASS for
all twelve stages: nine default exact/symbolic stages, two optional formula
audits, and one reference-bound Ethos proof check. All twelve subprocess
return codes are zero; the report records zero SMT queries, one fresh kernel
proof, and restored archived bytes. The primary-text skip is visible in the
actual refreshed subprocess stdout. This review inspected that saved evidence
rather than repeating the same checks.

No mathematical correction or dependency/skip-semantic blocker remains in
the reviewed public presentation. The one six-shape CPC box and one analytic
four-base box are partial exclusions. The exceptional negative branch, other
height charts, continuous coverage, external priority, and global conjecture
remain unresolved. The 58 necessary contact-type count is unchanged.

The final snapshot manifest, repository-wide privacy scan and GitHub push
are the publishing agent's separate checks; this review does not claim to
have performed those operations or verified a remote commit.
